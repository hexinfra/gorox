NAMING
======

  Explicit private names
  ----------------------

      _adoptMatch
      _adoptSince

  Special types
  -------------

    as mixin types:

      webServer_
	  _client_


COMPONENTS
==========

  stage [*]
    fixture (clock, fcache, resolver, happOutgate, http1Outgate, http2Outgate, http3Outgate, quicOutgate, tcpsOutgate, udpsOutgate)
    runner (...)
    backend (http1Backend, http2Backend, http3Backend, quicBackend, tcpsBackend, udpsBackend)
    router (quicRouter, tcpsRouter, udpsRouter)
      dealer (...)
      editor (...)
      case [*]
    stater (localStater, redisStater, ...)
    cacher (localCacher, redisCacher, ...)
    app [*]
      handlet (hello, static, ...)
      reviser (gzip, head, ...)
      socklet (hello, ...)
      rule [*]
    svc [*]
    server (httpxServer, http3Server, echoServer, ...)
    cronjob (clean, stat, ...)
