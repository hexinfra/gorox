NAMING
======

  Explicit private names
  ----------------------

      _adoptMatch
      _adoptSince

  Special types
  -------------

    as mixin types:

      webServer_
	  _client_


COMPONENTS
==========

  stage [*]
    fixture (clock, fcache, resolv, http1Outgate, tcpsOutgate, ...)
    addon (...)
    backend (http1Backend, tcpsBackend, ...)
    mesher (quicMesher, tcpsMesher, udpsMesher)
      filter (...)
      case [*]
    stater (localStater, redisStater, ...)
    cacher (localCacher, redisCacher, ...)
    app [*]
      handlet (hello, static, ...)
      reviser (gzip, head, ...)
      socklet (hello, ...)
      rule [*]
    svc [*]
    server (httpxServer, http3Server, echoServer, ...)
    cronjob (clean, stat, ...)
