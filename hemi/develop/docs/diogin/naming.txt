NAMING
======

  Explicit private names
  ----------------------

      _adoptMatch
      _adoptSince

  Special types
  -------------

    as mixin types:

      hRequest_
      webServer_
	  _client_


COMPONENTS
==========

  stage [*]
    fixture (clock, fcache, resolver, http1Outgate, http2Outgate, http3Outgate, quicOutgate, tcpsOutgate, udpsOutgate, unixOutgate)
    uniture (...)
    backend (http1Backend, http2Backend, http3Backend, quicBackend, tcpsBackend, udpsBackend, unixBackend)
    mesher (quicMesher, tcpsMesher, udpsMesher)
      filter (...)
      editor (...)
      case [*]
    stater (localStater, redisStater, ...)
    cacher (localCacher, redisCacher, ...)
    app [*]
      handlet (hello, static, ...)
      reviser (gzip, header, ...)
      socklet (hello, ...)
      rule [*]
    svc [*]
    server (httpxServer, http3Server, echoServer, ...)
    cronjob (clean, ...)
