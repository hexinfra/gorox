This directory holds microservices of gorox, basically one svc per directory.

Microservice support is under design, currently you can only implement them through apps.

An RPC server can host many svcs, whereas a svc can be bound to many RPC servers.

Stay tuned.
