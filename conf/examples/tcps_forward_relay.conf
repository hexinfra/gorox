// shell> ./gorox serve -conf conf/examples/tcps_forward_relay.conf

// Clients MUST use SOCKS to connect to this router.
// NOTE: public SOCKS relays are dangerous to your internal network!

stage {
	// routers
	tcpsRouter "tcps" {
		.address = ":8888"

		case { // matches all
			tcpsRelay {
				.relayMode = "socks"
			}
		}
	}
}
