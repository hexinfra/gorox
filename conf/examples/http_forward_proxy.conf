// shell> ./gorox -config conf/examples/http_forward_proxy.conf

stage {
	// fixtures
	http1Outgate {
		.tlsMode = false
		.maxStreamsPerConn = 10
		.maxContentSize = 1T
	}

	// webapps
	webapp "proxy" {
		.hostnames = ("*")

		rule { // matches all
			http1Proxy {
				.proxyMode = "forward"
			}
		}
	}

	// servers
	httpxServer "main" {
		.forApps = ("proxy")
		.address = ":8080"
	}
}
