// Config for Goops.

stage {
	.logFile = %logsDir + "/goops-worker.log"

	app "webui" {
		.hostnames = ("*") // change to your hostnames
		.webRoot   = %baseDir + "/cmds/goops/webui/root"

		rule $path == "/favicon.ico" {
			favicon {}
		}
		rule {
			webuiHandlet "logic" {
				.sites = [
					"front": [
						"hostnames": ("*"),
						"viewDir": %baseDir + "/cmds/goops/webui/view",
					],
				]
			}
		}
	}
	app "iface" {
		.hostnames = ("127.0.0.1") // change to your hostnames

		rule {
			v1Handlet {}
		}
	}

	httpxServer "web" {
		.forApps = ("webui", "iface")
		.address = ":9080"
	}

	rocksServer "cli" {
		.address = ":9090"
	}

}
