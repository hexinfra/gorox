// shell> ./gorox serve -config conf/examples/http_reverse_proxy.conf

stage {
	// backends
	http1Backend "h1b" {
		.balancer = "roundRobin"
		.nodes    = (
			["address": "127.0.0.1:3080", "weight": 1],
			["address": "127.0.0.1:3090", "weight": 2],
		)
	}

	// apps
	app "proxy" {
		.hostnames = ("*")
		.tlsCertificate = "" // if TLS
		.tlsPrivateKey  = "" // if TLS

		rule { // matches all
			http1Proxy {
				.toBackend = "h1b"
			}
		}
	}

	// servers
	httpxServer "main" {
		.forApps = ("proxy")
		.address = ":80"
	}
	httpxServer "safe" {
		.forApps = () // add "proxy" if needed. remember to set certificate file and private key file in app
		.address = ":443"
		.tlsMode = true
	}
}
